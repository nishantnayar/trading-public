[theme]
base = "dark"
primaryColor = "#2dd4bf"
backgroundColor = "#0a0e12"
secondaryBackgroundColor = "#12181f"
textColor = "#e6edf3"
font = "monospace"

[server]
headless = true
# Dedicated port so quantis never collides with other local Streamlit apps.
port = 8502
